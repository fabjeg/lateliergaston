# MongoDB Atlas connection string
# Get this from MongoDB Atlas Dashboard → Connect → Connect your application
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/lateliergaston

# Session secret for authentication (generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
SESSION_SECRET=your_session_secret_here

# Stripe public key (already in .env or set in Vercel)
VITE_STRIPE_PUBLIC_KEY=pk_test_...
